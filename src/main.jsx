import React from 'react'
import ReactDOM from 'react-dom/client'
import UserList from './project/use-memo-form'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    {/* <App /> */}
    <UserList/>
  </React.StrictMode>,
)
